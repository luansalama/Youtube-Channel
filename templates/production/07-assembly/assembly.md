# Montagem

NLE de produção: Adobe Premiere Pro via MCP (`studio/nle.json`).
Handoff: `.studio/internal/assembly/premiere-pro/`.

A montagem live só ocorre após `cutlist_lock`; readback da timeline é obrigatório.
